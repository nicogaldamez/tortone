<tr id="<%= dom_id(brand) %>">
  <td> <%= link_to brand.name, edit_brand_path(brand) %> </td>
  <td>
    <%= link_to 'Editar', edit_brand_path(brand), class: 'btn btn-xs btn-primary' %>
    <%= link_to 'Eliminar', brand, method: :delete,
        data: {confirm: '¿Está seguro?'}, class: 'btn btn-xs btn-danger' %>
  </td>

</tr>

