<tr id="<%= dom_id(sale) %>" data-sale-id="<%= sale.id %>" class="<%= sale.klass %>">

  <td>
    <%= link_to sale.vehicle.decorate.identification, sale %>
   </td>
  <td> <%= sale.sold_on %> </td>
  <td class="text-center">
    <%= sale.vehicle_cost %>
  </td>
  <td class="text-center"> <%= sale.price %> </td>
  <td class="text-center"> <%= sale.cash %> </td>
  <td class="text-center"> <%= sale.transfer_amount %> </td>
  <td class="text-center"> <%= sale.expenses %> </td>
  <td class="text-right"> <%= sale.profit %> </td>
  <td>
    <%= link_to fa_icon(:pencil), edit_sale_path(sale), title: 'Editar los datos de la Venta', class: 'btn btn-xs btn-primary' %>
    <%= link_to fa_icon(:trash), sale, method: :delete,
        data: {confirm: '¿Está seguro?'}, title: 'Eliminar Venta', class: 'btn btn-xs btn-danger' %>
    <%= sale.customer_btn %>
  </td>

</tr>
