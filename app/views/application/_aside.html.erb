<aside class="main-sidebar">
  <section class="sidebar">

    <ul class="sidebar-menu">

      <%= side_link 'Inicio', root_path, 'home' %>

      <% if logged_in? %>
        <%= side_link 'Clientes', customers_path, 'users' %>
        <%= side_link 'Vehículos', vehicles_path, 'car' %>
        <%= side_link 'Mis vehículos', 
                  vehicles_path(vehicle_filter: {is_owner: true}), 'car' %>
      <% end %>

    </ul>

  </section>
</aside>
